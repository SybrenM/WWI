SELECT COUNT(*), cursus
FROM inschrijving
 GROUP BY cursus
 
 
SELECT avg(maandsal), afd 
FROM medewerker
 GROUP BY afd

/!\ SELECT COUNT(inschrijving.cursus) 'totale inschrijving', cursus.omschrijving
FROM inschrijving
INNER JOIN uitvoering ON inschrijving.cursus = uitvoering.cursus
INNER JOIN cursus ON cursus.code = uitvoering.cursus
GROUP BY cursus.omschrijving
HAVING COUNT(*) < 5

SELECT mnr, naam, COUNT(mnr)
from medewerker
JOIN uitvoering on uitvoering.docent = medewerker.mnr
GROUP BY mnr

/!\SELECT COUNT(DISTINCT inschrijving.cursist), AVG(inschrijving.evaluatie)
from inschrijving
INNER JOIN uitvoering on uitvoering.cursus = inschrijving.cursus
GROUP BY uitvoering.begindatum
having uitvoering.begindatum > '1995-12-31'


SELECT L.mnr, L.naam, L.maandsal
from medewerker L
WHERE L.maandsal > (SELECT M.maandsal FROM medewerker M WHERE M.mnr = L.chef)

SELECT L.mnr, L.naam, L.gbdatum, L.afd, afdeling.hoofd
from medewerker L
 INNER JOIN afdeling on L.afd = afdeling.anr
WHERE L.gbdatum < (SELECT M.gbdatum FROM medewerker M WHERE afdeling.hoofd = M.mnr)

?????

/!\select *
 from medewerker L
 INNER JOIN afdeling on L.afd = afdeling.anr 
WHERE L.maandsal > (SELECT AVG(M.maandsal) FROM medewerker M)

SELECT cursus.omschrijving, cursus.code, M.docent, M.begindatum
FROM uitvoering M
inner join cursus on M.cursus = cursus.code
GROUP BY M.cursus
HAVING M.begindatum IN (SELECT MIN(U.begindatum) FROM uitvoering U group by U.cursus)
/?\SELECT cursus.omschrijving, cursus.code, M.docent, M.begindatum
FROM uitvoering M
inner join cursus on M.cursus = cursus.code
WHERE M.begindatum IN (SELECT MIN(U.begindatum) FROM uitvoering U group by U.cursus)




EXTRA

SELECT M.mnr 'is chef', C.naam 'chef van', C.mnr
FROM medewerker M
INNER JOIN medewerker C on M.mnr = C.chef
ORDER BY M.mnr

SELECT M.mnr 'is chef', COUNT(C.chef)
FROM medewerker M
INNER JOIN medewerker C on M.mnr = C.chef
GROUP BY M.mnr

SELECT M.mnr 'is chef', M.naam, C.naam 'chef van', C.mnr
FROM medewerker M
INNER JOIN medewerker C on M.mnr = C.chef
ORDER BY M.mnr


SELECT M.mnr 'is chef', M.naam, COUNT(C.chef)
FROM medewerker M
INNER JOIN medewerker C on M.mnr = C.chef
GROUP BY M.mnr


SELECT M.mnr, M.naam, C.mnr, C.naam
FROM medewerker M
LEFT JOIN medewerker C ON M.mnr = C.chef


SELECT M.mnr, M.naam, COUNT(C.mnr)
FROM medewerker M
LEFT JOIN medewerker C ON M.mnr = C.chef
GROUP BY M.mnr


SELECT M.mnr, COUNT(C.mnr)
FROM medewerker M
LEFT JOIN medewerker C ON M.mnr = C.chef
GROUP BY M.mnr
HAVING COUNT(C.mnr) > 2

SELECT M.mnr, M.naam, COUNT(C.mnr)
FROM medewerker M
LEFT JOIN medewerker C ON M.mnr = C.chef
GROUP BY M.mnr
HAVING COUNT(C.mnr) > 2


SELECT M.mnr, M.naam, COUNT(C.mnr)
FROM medewerker M
LEFT JOIN medewerker C ON M.mnr = C.chef
GROUP BY M.mnr
HAVING COUNT(C.mnr) < 2


